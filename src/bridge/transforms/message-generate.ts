import * as PUPPET from 'wechaty-puppet';

const urlMessageGenerate = (urlLinkPayload: PUPPET.payloads.UrlLink) => {
  const { title, url, thumbnailUrl, description } = urlLinkPayload;
  return ``;
};
